export interface CooperationOption {
  icon: string;
  title: string;
  description: string;
}

export interface PromiseItem {
  value: string;
  label: string;
}

export interface CollaborationBrand {
  name: string;
  logoUrl: string;
}
